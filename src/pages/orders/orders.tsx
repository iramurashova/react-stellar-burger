import React, { FC } from "react";
import styles from "./orders.module.css";
const OrdersPage: FC = () => {
  return (
    <h1 className={`text text_type_digits-medium ${styles.orders}`}>
      Эта страница пока недоступна
    </h1>
  );
};

export default OrdersPage;
