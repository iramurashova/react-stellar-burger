import React, { FC } from "react";
import styles from "./history.module.css";
const HistoryPage: FC = () => {
  return (
    <div className={`text text_type_digits-large ${styles.history}`}>
      Not found
    </div>
  );
};

export default HistoryPage;
